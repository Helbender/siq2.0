import { defineRecipe } from "@chakra-ui/react";
export const inputRecipe = defineRecipe({
  base: {
    // field: {
    borderRadius: "md",
    border: "1px solid",
    borderColor: "red",
    color: "text.primary",
    bg: "bg.canvas",
    // },
  },
});
